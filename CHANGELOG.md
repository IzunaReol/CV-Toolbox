# 更新日志 / Changelog

本文件记录 CV Toolbox 各版本的主要变化。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

---

## [Unreleased]

下一版本的变更将在此记录。

---

## [v2.2.0] - 2026-09-05

### 新增 (Added)

- 源视频帧率与自定义帧率支持小数；选择原视频帧率时直接使用源 FPS，不按抽帧间隔换算。
- 所有上传框增加独立的“清空已上传”按钮，无需刷新即可重复使用；清空模型同步重置类别选择。
- 增加可选流式全流程，仅保存最终视频和推理统计，减少中间图片 IO。

### 修复 (Fixed)

- 图片任务及批次 ID 增加随机后缀，避免同一秒连续提交重名。
- 运行中禁止清空本地文件或删除工件；清理失败明确报告未删除项。
- 每次视频上传分配独立 ID；重复运行使用新输出目录，避免同名同大小视频复用旧内容、历史帧混入。
- 上传模型使用完整 SHA-256 寻址并原子写入，同名新权重不会改变运行中批次使用的模型。
- 统一 JPG/JPEG/PNG/BMP/WebP/TIF/TIFF 输入、计数、浏览和 ZIP 下载。
- 合成视频先写临时文件，检查编码器、文件、帧数及首尾帧后才发布；失败或取消清理临时文件，保留已有成品。
- 合成时损坏或尺寸不一致的图片明确失败，不再静默跳帧；纯损坏输入不能标记推理成功。
- 抽帧 CLI 实现 `--video` / `--output` / `--interval` 参数校验，异常时释放视频；CLI 拒绝复用已有帧或标注图片的目录。
- 任务时间固定按东八区显示，避免 Windows 与 GitHub Actions 使用不同时区时出现差异。

### 性能 (Performance)

- 一个批次复用一个模型；图片解码一次后同时供推理和绘图使用，字体按字号缓存。
- 批次进度每 0.5 秒最多保存一次，阶段切换与终态立即保存；减少逐帧日志。
- 新任务提交时回收已完成的后台 Future 引用。

### 验证 (Validation)

- 本地 50 项回归测试通过，覆盖上传框重复清空、任务隔离、模型不可变、跨格式 ZIP、真实 MP4、源/自定义帧率、流式取消、异常编码器、删除保护及 CLI。
- 语法编译、Ruff 检查及格式检查通过；测试使用替代检测模型，不代表真实 GPU 性能验证。

### 工程 (Engineering)

- 增加依赖自动更新和 CodeQL 安全检查配置。
- 完善仓库元数据；同步整理 README、工作流、验收清单与 Release 正文。

---

## [v2.1.2] - 2026-08-29

### 修复 (Fixed)

- 修复 Windows 中文路径下图片写入报告成功但文件未实际生成的问题，解决仅抽帧结果为 0、下载 ZIP 为空，并同步保障推理标注图正常落盘。
- 图片保存失败或视频无法打开时现在会明确标记任务失败，不再显示为已完成。

### 变更 (Changed)

- 任务进度和处理结果统一在任务区块内展示任务名称、中文任务类型和开始时间。
- 任务开始时间统一使用 `YYYY-MM-DD HH:MM:SS` 格式。

---

## [v2.1.1] - 2026-08-29

### 变更 (Changed)

- 任务进度中的处理阶段统一显示为中文，文件浏览模式不再展示任务进度区域。
- 处理结果标题增加任务开始时间，类别统计表统一居中显示。
- 单视频全流程不再显示额外的批量 ZIP 下载入口，多视频任务仍可统一打包。
- 文件浏览删除当前预览图片后自动切换到下一张，连续整理图片更加顺畅。

---

## [v2.1.0] - 2026-08-29

### 新增 (Added)

- 处理任务改为后台串行执行，支持持续查看进度、刷新后恢复跟踪和取消当前批次。
- 每个任务保存运行配置、状态和推理统计，文件浏览可直接查看任务摘要。
- 推理结果增加任务汇总和逐图统计，可供后续分析使用。
- 推理类别统计改为表格展示，类别和数量更加直观。
- 大图预览增加关闭、选择、下载、单图删除、图片信息和跨页导航。
- ZIP 准备和批量删除增加进度及结果反馈。

### 变更 (Changed)

- 文件浏览使用带修订号的扫描缓存，普通选择和翻页不再重复遍历全部工件。
- 任务状态区域移动到处理结果上方并改用更直观的名称；页面重置后不再恢复已结束批次。
- 文件浏览中的任务模式统一显示为中文名称。
- 删除文件后自动清理空目录；任务无剩余工件时一并移除空任务。
- 损坏或未写完的图片改为显示不可预览提示，不再中断整个页面。
- GitHub Actions 更新为轻量导入和单元测试流程，不再依赖安装 Torch。
- README 和工作流文档同步更新为 v2.1.0。

---

## [v2.0.1] - 2026-08-26

### 新增 (Added)

- 图片缩略图支持点击选择或取消选择，并通过选中样式区分当前状态。
- 图片大图预览增加上一张和下一张导航。

### 变更 (Changed)

- 推理类别按钮文案调整为「全部选择」和「全部取消」。
- 文件选择和页码控件移动到图片分页信息下方。
- 选择图片后保留当前页码，不再自动跳回第一页。
- 文件浏览自动隐藏空任务和没有文件的目录，只展示可直接浏览的工件目录。
- 左侧推理设备选项精简为 `auto`、`cpu` 和 `cuda`。

---

## [v2.0.0] - 2026-08-26

### 新增 (Added)

- 模型推理支持选择一个或多个类别，CLI 可使用类别 ID 或名称指定过滤范围。
- Web 推理设置增加类别多选，以及全选和全取消操作。
- 新增文件浏览模式，可查看 `outputs/` 下的任务目录和工件。
- 支持图片分页预览、文件多选下载和二次确认删除。
- 多选超过 5 个文件或总大小超过 20 MiB 时自动打包 ZIP。

### 变更 (Changed)

- 推理类别默认全部选中；未选择任何类别时不允许启动推理。
- 模型切换后自动刷新类别列表和选择状态。
- 项目介绍、工作流和使用示例更新为 v2.0.0。

---

## [v1.0.6] - 2026-08-25

### 新增 (Added)

- 新增页面重置功能，可清空当前页面状态并重建上传控件，不影响本地文件。
- 为主要页面控件补充独立状态标识，提升多次操作和页面刷新后的稳定性。
- 下载文件增加本地缓存，相同结果可直接复用。

### 修复 (Fixed)

- 修复上传模型后，页面交互可能因重复读写模型文件而变慢或无响应的问题。
- 修复上传控件卡在加载状态后难以恢复的问题。
- 修复重复使用页面重置功能时偶尔不生效的问题。
- 修复下载文件时触发整页刷新，导致页面无响应或多个下载请求积压的问题。
- 修复结果区在展示下载按钮时提前读取视频、压缩图片包造成的卡顿。
- 修复仅合成模式下源视频上传框的显示逻辑：使用原视频帧率时显示，自定义帧率时隐藏。
- 降低 Windows 环境下上传或下载中断时的连接异常频率。

### 变更 (Changed)

- 「清空缓存」更名为「重置页面」，功能仅作用于当前页面和会话状态。
- 删除上传框旁的「更换」按钮，统一通过页面重置功能恢复上传状态。
- 视频和 ZIP 文件改为点击下载时再准备，避免影响页面正常操作。
- ZIP 结果缓存在 `outputs/_downloads/`，源文件未变化时不再重复生成。
- 固定 Web 服务相关依赖版本，减少环境升级带来的行为差异。

---

## [v1.0.5] - 2026-07-20

### 新增 (Added)

- 更换或删除模型时，自动清理上一个模型的类别信息和标注名称。

### 变更 (Changed)

- 简化文件上传后的状态展示。
- 单步骤模式不再显示「下载全部」，仅保留当前结果的独立下载入口。
- 「清空会话」更名为「清空缓存」。

---

## [v1.0.4] - 2026-07-20

### 新增 (Added)

- 上传控件增加重新选择文件的入口。
- 上传完成后显示文件名称和大小。
- 处理期间增加整体加载提示。
- README 增加上传中断和连接异常相关说明。

### 变更 (Changed)

- Web 启动命令建议的最大上传大小调整为 1024 MB。

---

## [v1.0.3] - 2026-06-11

### 新增 (Added)

- 侧栏增加「清空本地文件」功能，并提供二次确认。
- 页面版本号改为从项目配置中自动读取。

### 变更 (Changed)

- 全流程结果区仅展示最终视频，不再展示中间帧和标注图片。
- 全流程的「下载全部」仅包含最终视频。
- 清空会话时同步清理本次上传的文件和临时模型。
- 调整仅合成模式的帧率和源视频选择逻辑。

---

## [v1.0.2] - 2026-05-14

### 新增 (Added)

- 「仅推理」支持直接上传一张或多张图片。
- 「仅合成」支持直接上传图片并生成视频。
- 仅抽帧和仅推理结果支持单独下载 ZIP。
- 结果区增加「清空结果」按钮，仅清理当前页面展示。

### 修复 (Fixed)

- 修复「下载全部」包含历史任务结果的问题，现在只打包当前会话内容。

### 变更 (Changed)

- 单步骤任务使用时间戳自动生成输出目录名称。
- 三个单步骤模式可独立使用，不再依赖前一步产生的目录。

---

## [v1.0.1] - 2026-05-12

### 新增 (Added)

- 增加全流程、仅抽帧、仅推理和仅合成四种运行模式。
- 抽帧间隔、置信度和 IoU 支持手动输入与无效值回退。
- 标注框颜色支持中文选项和自定义颜色。
- 支持读取模型类别，并为每个类别设置自定义显示名称。
- 合成视频支持使用原视频帧率或自定义帧率。
- 多视频任务增加独立的进度和状态展示。

### 修复 (Fixed)

- 修复多视频上传后任务名称冲突、最终只保留一个结果的问题。

### 变更 (Changed)

- 页面标题调整为「CV 工具箱」。
- 上传视频的内部文件名统一为 `video.mp4`。

---

## [v1.0.0] - 2026-05-11

### 新增 (Added)

- 提供视频抽帧、YOLO 模型推理和图片合成视频的完整流程。
- 提供三段式命令行脚本，可分别执行抽帧、推理和视频合成。
- 提供 Streamlit Web 界面，支持视频和模型上传、参数配置、处理进度及结果下载。
- 支持多视频处理、自定义检测框颜色和类别名称。
- 支持中文路径和中文标签显示。
- 项目采用 MIT License 发布。
