# v2.2.0 手工验收与发布准备

发布批准：2026-09-05 用户已确认执行发布。自动化验证通过；下表保留手测场景供后续回归，未逐项记录手测结果。正式版本页面见 [v2.2.0 Release](https://github.com/IzunaReol/CV-Toolbox/releases/tag/v2.2.0)。

## 启动

如旧版服务仍运行，先停止再启动，确保后台工作线程和模块加载的是新版本：

```bash
python -m streamlit run web/app.py --server.maxUploadSize 1024
```

页面顶部应显示 `v2.2.0`。准备一个可信的 YOLO 检测权重、两个短视频，以及 PNG/JPEG/WebP 图片。

## 验收清单

| 状态 | 操作 | 预期 |
|---|---|---|
| 待测 | 同一个视频连续运行两次，第二次增大抽帧间隔 | 两次任务目录不同；第二次帧数降低，第一次结果不变 |
| 待测 | 分别上传两个同名视频；可额外使用同大小但内容不同的文件 | 每次处理的是本次文件，目录独立，不复用旧内容 |
| 待测 | 多视频批次运行期间，上传另一份同名模型 | 当前批次始终使用提交时权重；新模型只影响下次任务 |
| 待测 | 仅推理上传纯 PNG、纯 JPEG 和混合格式图片 | 可以完成，计数正确；ZIP 中包含全部成功标注图片 |
| 待测 | 混合正常图片与一个损坏图片执行仅推理 | 正常图完成，失败数量明确；全是损坏图片时失败 |
| 待测 | 使用间隔 5，选择原视频帧率 | 界面没有“播放方式”；输出 FPS 等于源 FPS，时长约为原视频五分之一 |
| 待测 | 使用 29.97 FPS 源视频和自定义 23.976 FPS | 输出帧率保留小数，不强制取整 |
| 待测 | 勾选“仅保存最终视频”，运行一个视频和多个视频 | 生成可播放视频与统计；无 frames/annotated 图片目录；多个结果提供批量 ZIP |
| 待测 | 标准流程和流式流程分别点击停止，再刷新页面 | 正确显示已取消；标准流程中间图片保留，流式半成品视频清理；可以提交下一批 |
| 待测 | 仅合成上传损坏或不同尺寸图片 | 明确失败，不显示成功视频，不静默跳帧 |
| 待测 | 各上传框连续上传、点“清空已上传”、重新上传两次 | 仅当前框清空，其他框、参数、结果保留，不需要刷新浏览器 |
| 待测 | 清空模型框 | 模型路径与类别选择重置，已提交任务继续执行 |
| 待测 | 运行中点击清空本地文件或在文件浏览中删除工件 | 提示完成或取消后再删除，处理中数据保留 |
| 待测 | 下载视频、图片 ZIP、批量 ZIP，打开文件浏览 | 下载完整，历史结果仍可访问，界面不因下载整页重跑 |
| 待测 | 执行下面的抽帧命令，再尝试原目录重跑 | 第一次按参数成功；第二次明确要求使用新目录 |

```bash
python scripts/1_frame_extract.py --video <视频路径> --output outputs/cli_v220_test/frames --interval 5
```

## 自动化验证

最近一次本地验证：50 项测试通过，语法编译、Ruff 检查、格式检查与 Git 空白检查通过。真实模型/GPU 手测结果未逐项记录。

```bash
python -m unittest discover -s tests -p "test_*.py"
python -m compileall -q web scripts tests
python -m ruff check web scripts tests
python -m ruff format --check web scripts tests
git diff --check
```

回归测试使用临时生成的真实图片和 MP4、替代检测模型；不下载模型、不测试实际 GPU 吞吐。Ruff 需先通过 `python -m pip install ruff` 安装。

## 用户确认通过后的发布步骤

目标仓库：<https://github.com/IzunaReol/CV-Toolbox>。

1. 记录手测结果；将 CHANGELOG 中 v2.2.0 的“待发布”改为实际发布日期，同步本清单的发布状态。
2. 核对版本号、文档链接和 Git 差异；确认提交范围仅包含源码、测试、配置与文档，不包含 `uploads/`、`outputs/`、权重或临时日志。
3. 检查远程最新状态和主分支规则，执行必要测试后提交与推送；按仓库规则使用 PR。
4. 确认最终发布提交及 CI 通过，创建 `v2.2.0` 标签。
5. **创建 GitHub Release**，标题 `CV Toolbox v2.2.0`，正文使用 [发布说明](releases/v2.2.0.md)，不能只推送标签。
6. 核验 Release 页面、标签和提交一致，向用户提供 Release 链接。

建议提交标题：`feat: release CV Toolbox v2.2.0`。发布前应将全部关联源码、测试和文档纳入同一次版本变更。
